# AI 交接

## 项目目的

开发一个 macOS 程序，识别全局快捷键和当前前台 App 的快捷键，用虚拟键盘高亮按键，并在旁边标明组合键及作用。

## 背景与当前判断

- 用户希望先确认 GitHub 是否已有相近项目。
- 已完成首轮 GitHub 调研，详见 `GitHub同类项目调研.md`。
- 已发现“快捷键清单”和“按键可视化”两类成熟实现，但未发现把目标能力完整合并的成熟开源项目。
- 核心技术风险是“全局快捷键”没有一个可枚举所有第三方注册项的统一公开接口，产品必须明确数据来源与覆盖率。

## 当前实现方式

- 已完成 SwiftUI/AppKit MVP，产物为 `dist/快捷键地图.app`。
- 通过 Accessibility API 读取最近使用的前台 App 菜单快捷键。
- 支持虚拟键盘高亮、精确修饰键组合筛选、动作/组合键/菜单路径搜索和来源展示。
- 使用 Swift Package 构建；当前机器没有完整 Xcode，构建脚本负责组装并本地签名 `.app`。
- 已增加按住 `⌃⌥Space` 唤起、松开隐藏的全局监听。
- 唤起机制已从 `NSEvent` 全局监听改为 Carbon `RegisterEventHotKey`，无需输入监控权限；处理 `kEventHotKeyPressed` 与 `kEventHotKeyReleased`。
- 浮层使用非激活 `NSPanel`、透明材质和浮动窗口层级，不切换当前 App 焦点。
- 浮层左侧展示三条重点引线（App 专属规则优先），右侧“完整目录”按修饰键分组列出全部已识别快捷键，不再截断。
- App 专属目录已接入 ChatGPT（bundle ID `com.openai.codex`）：`⌃⇧M → 切换模型`。

## 验证结果

- `swift test`：2 项测试通过，覆盖组合键顺序与键名标准化。
- Release 构建、临时签名和 `.app` 启动通过。
- 实际界面回读通过：权限引导、完整键盘、功能列表均正常显示。
- 交互验证通过：从 `⌘` 切换到 `⇧⌘` 后，仅显示并高亮 `⇧⌘Z → 重做`。
- 浮层视觉回读通过：窗口层级为 5，玻璃面板正常显示；`⌃`、`⇧`、`M` 三条引线汇聚到“ChatGPT 切换模型”。
- 2026-08-12 用户反馈实体按键未唤起。诊断确认原方案缺少可靠的全局事件权限/事件链；已改为系统热键注册。
- 当前版本热键注册返回 `OSStatus 0`，证明 `⌃⌥Space` 已被系统成功注册；事件处理器数组指针已按 Carbon API 的连续内存要求修正。
- 2026-08-12 再次确认授权无法持久：TCC 仍绑定旧 CDHash `2170…`，当前构建为 `baf265…`；仅切换旧权限条目不会更新代码要求。
- 已将当前构建安装到固定路径 `/Applications/快捷键地图.app`，签名/CDHash 验证为 `baf265…`，清除旧 Accessibility 条目后从固定路径启动。后续验收前不得替换该应用。
- 2026-08-12 用户指出浮层未展示全部快捷键；根因是视图显式使用 `prefix(3)`。已改为重点引线 + 完整分组目录，目录计数等于 `model.shortcuts.count`。
- 构建签名已加入稳定 designated requirement：`identifier "com.kanyun.shortcutmap"`，避免未来二进制更新让 TCC 只绑定变化的 CDHash。
- 2026-08-12 浮层增加按鼠标所在屏幕 `visibleFrame` 自适应缩放，四周保留 24/20pt 安全边距。当前内建屏可用区域 1280×800，实测缩放 0.780，面板 1232×639，可完整容纳。
- 已确认 `/Applications/快捷键地图.app` 签名与运行正常；重复的 `dist/快捷键地图.app` 已移到废纸篓，项目内 `dist` 空目录已移除。
- App Icon 已按 macOS 27 Liquid Glass 方向设计并集成：浅蓝/长春花紫背景，三个 Mac 修饰键 `⌘`、`⌥`、`⇧` 汇聚到珊瑚色作用节点；母版与 `.icns` 位于 `Resources/`。
- 快捷键模型已增加作用域：`全局` / `当前 App` / `输入法`。浮层目录先按来源分区，再按修饰键组合分组。
- 当前可验证来源：全局包含快捷键地图自身 `⌃⌥Space`；当前 App 包含 AX 菜单与 App 内置规则；输入法显示当前名称，未验证到配置时明确显示 0 条，不猜测。
- 浮层由 1580×820 压缩到 1580×680；内建屏实际面板 1392×600。左侧重点图与右侧完整目录重新分配为 820/690，视觉回读确认大面积空白已显著减少。
- 2026-08-12 根据用户截图修正左侧布局：原三张说明卡覆盖键盘右侧，造成 `[]\\`、回车等视觉截断。现改为完整键盘在上、三张卡片在下横排，引线向下连接；左/右宽度调整为 900/610。实际回读确认键盘和方向键完整，下方空白已被卡片占用。
- 2026-08-12 已增加微信输入法配置扫描：当当前输入法为微信输入法/WeType 时，解析 `~/Library/Application Support/WeType/mmkv/wetype.settings`，读取语音按住说话与语音免提切换的启用状态、实际键码；支持单独 `Fn/Shift/Control/Option/Command` 及 `Fn + Space`。虚拟键盘已补 `Fn`。真实配置回读确认当前 `⇧ → 按住语音输入` 已显示，来源为“微信输入法设置 › 语音输入”；7 项测试通过。
- 2026-08-12 首次授权失败已定位：用户授权记录绑定旧临时签名 CDHash `9cb3…`，当前构建为 `89b0…`，TCC 日志明确报 `Failed to match existing code requirement`。
- 已执行 `tccutil reset Accessibility com.kanyun.shortcutmap` 清除失效授权，并在不重新构建的前提下重启当前 App，等待用户再次勾选后验收。
- 2026-08-12 主界面增加 App 选择器：默认“跟随当前 App”，也可从正在运行的常规 App 中选择并锁定查看对象；目标 App 退出后自动恢复跟随模式。
- 新功能已通过 7 项 `swift test`、Release 构建与实际主界面回读；项目内测试包为 `dist/快捷键地图.app`。未覆盖 `/Applications` 版本，以免打断当前 TCC 授权验收。
- 2026-08-12 修复 App 选择器图标裁切：原因是 `NSImage` 按原始尺寸参与 Picker 布局；现统一为 18×18 等比缩放。已重新通过 7 项测试、Release 构建和主界面回读。

## 未解决问题

1. 用户需对 `/Applications/快捷键地图.app` 的全新权限条目授权，并完成持久性及真实菜单读取验收。
2. 当前是 ad-hoc 临时签名，每次二进制变化都会改变 TCC 身份；继续迭代前需建立稳定的本地代码签名证书，或接受每次构建后重新授权。
3. `⌃⌥Space` 当前是固定唤起键，尚需用户在实体键盘完成按住/松开验收，尚无用户设置界面。
4. 全局快捷键数据源尚未接入，需按 macOS / Raycast / Alfred / Keyboard Maestro / skhd 分层实现。
5. ANSI 键盘已实现，ISO/JIS 和数字键盘布局尚未实现。

## 下一步

用户授权辅助功能后，先用 Finder、Edge 和一个 Electron App 做真实菜单读取验收；通过后增加全局唤起键与系统全局快捷键来源。
